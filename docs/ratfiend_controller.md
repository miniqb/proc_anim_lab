# RatFiend（鼠煞）并列后端 —— 驼背鼠头人形怪：姿态混合、断肢与爬行

> 第 11 个并列物种后端（2026-08-22）。参考形象：苍白灰皮、修长瘦削的鼠头人形怪——
> 长吻大嘴满口尖牙、大立耳、重度驼背、双手下垂利爪、小尾巴（用户提供的恐怖谷参考图）。
> 机制基底 = Humanoid（清醒近地失重伺服木偶，零 knockdown 状态机），物种新造三块：
> **常态驼背**（倾斜站立力偶）、**走↔跑连续姿态混合**（Gait 标量）、**断肢与爬行**
> （固定断肘/膝 + 推进 ∝ 抓地肢体数），并提供宿主 opt-in 的**显式家具翻越意图**。
> 本项目**首个可动颌**渲染件。

## 1. 核心机制

### 1.1 装配（`RatFiendFactory`）

3 chunk `[胸, 髋, 头]` 照搬人形：胸↔髋 Rigid 杆（WeightA 质量反比）、胸↔头 PullOnly 脖
（允许压缩——「耷拉」的构件基础）；装配完显式钉定 RotationChunk（CLAUDE.md §6.3）。
不加第 4 chunk——驼背是平衡态**倾角**自由度，不是拓扑自由度。

- 腿 = 2 × Lizard `Limb`（唯一跨物种白名单边 `RatFiend>Lizard`，与 Humanoid 同款），
  anchor=髋，**LookaheadTicks>0 硬性**（双足前瞻点循环，Humanoid §3.5 结论；取值 3 是
  R11 步态定型——前瞻圈决定释放点，见 §5.9）、Pair 互绑、对角相位种子 + 控制器层
  反相自举（LegPhaseMinGap，§5.9）。修长感靠 LegLength 0.8 / ChestHipsDist 0.55 /
  半径小；LegSpeed 0.3 / StepLength 0.6 留在可行域（CLAUDE.md §6.5）。站姿几何
  （HipRideHeight ≈0.83×腿长、LegLateral 0.14 = R10 定型，FeetDown 4.5 = R11 定型）
  ——联动约束见 §5.8/§5.9，改任何一个前先读那两节。
- 臂 = 2 × 物种私有 `RatArm`（Humanoid `Arm` 的同构克隆 + `Severed` 标志 +
  `EffectiveLength` 断后减半）。**不白名单引 Humanoid.Arm**：断肢语义要改臂长钳制本体，
  为新物种动别家后端比动共享层更糟。
- 参数 `RatFiendParams` + `Snapshot()` 出生冻结（DropBugParams 模式）；
  `ById` 未知 ID 抛异常快速失败。

### 1.2 常态驼背 = 倾斜的站立力偶目标轴（显式，非阻尼差）

```
postureUp = normalize(worldUp·cosθ + Facing·sinθ)      // θ = HunchAngleDegrees（gaunt 25°）
Uprightness 与 ApplyStandCouple 全部改对 postureUp 度量/施力
```

失重伺服体制下站立力偶永远赢——把目标轴倾斜就是把**整个平衡态**倾斜：静止、走、跑
全程驼背（胸稳定停在髋前上方 sinθ×躯干长处）。头伺服/髋伺服/重力开关/撑地判定
仍用世界上方向。消融开关 `EnableHunchTilt=false` → 静立前倾塌回直立（smoke 红灯）。

**修复轮 R1（静立漂移）**：初版胸/髋阻尼取 0.9/0.86「近平」，实测静立 300 tick 净前漂
0.65m。根因：WeightedPush 每 tick 注入动量守恒，但**衰减系数不同的两端稳态速度不再抵消**
（v_i = a_i/(1−d_i)，倾斜力偶的水平分量被阻尼差整流成净动量 ≈0.002 m/tick）。
修复：胸/髋阻尼**逐字相等**（0.9/0.9）——稳态速度对回到动量零，漂移消失
（后半窗漂移 0.649 → 0.009m）。Humanoid 靠阻尼差做巡航弓背，本物种不需要它。

**修复轮 R2（行走驼背归零）**：阻尼拉平后行走驼背从 0.564 塌到 0.000——巡航时
BaseSpeed ≫ MaxMoveSpeed×(1−damping)，胸髋**双双被钉在同一推进天花板上**，任何力偶/
注入型前倾通道都被余量钳制吸收（Humanoid LeanPush 消融实证的同一现象）。
修复：**天花板本身不对称**（`HunchSpeedBias`：胸 ×(1+bias)、髋 ×(1−bias)）——钳制
就是通道，吸收不掉；刚性杆把持续速度差转成行进前倾。bias 定 0.02：静立 25°（dot 0.42）
→ 行走 32.5°（dot 0.54）的渐进（0.08 时 53° 成俯冲，被推翻）。R11 起天花板 ∝ RunSpeed
（§5.9 速度分离）——注入率与钳制余量同乘油门，「双双钉在天花板 + bias 差分」的论证
在每档油门下逐字保持。

### 1.3 走↔跑连续姿态混合（无 gait 枚举，涌现惯例）

进哈希的平滑标量 `Gait = LerpAndTick(Gait, 清醒有意图且可站立 ? RunSpeed : 0, 0.06, 0.01)`，
姿态权重 `r = InverseLerp(0.35, 0.95, Gait)`（End 取 0.95 不是 0.8——RunSpeed 0.8/1.0
两档的稳态姿态要可区分，smoke 单调断言）。r 驱动三路连续混合：

- **头耷拉↔抬头**：头伺服 aim = `lerp(norm(Facing−up·0.9), norm(Facing+up·0.08), r)`
  （前下 42° 耷拉 → 平视微仰）；爬行覆盖 `norm(Facing+up·0.2)`；攻击（GrabTarget 非空）
  覆盖为直视目标。轴外顶改沿 aim 顶（低头时顶向前下，防与伺服目标打架）。
- **垂手摆臂↔前伸欲抓**：手臂链走/跑分支按 r 混合目标点与追猎速度（§1.4）。
- **嘴微张↔大张**：`MouthOpen = clamp(0.15 + 0.55·r + MouthDrive)`（纯派生量不进哈希；
  `LastMouthOpen` 由 Gait 历史派生供渲染插值）。

Gait 必须内核平滑并进哈希：手臂目标点是物理量（RatArm 碰地形、断肢交接读它的 Pos/Vel），
姿态平滑交给渲染层会让物理端目标点瞬跳、双跑不定。`CrawlFactor` 同理（渲染 dorsal 混合/
肘 pole 翻转的连续驱动）。

### 1.4 手臂通道（TickArms 优先级链，Humanoid 扁平 if-else 形态）

每手固定序取第一条命中：① 昏迷→垂落；② 断臂→垂落（EffectiveLength 钳制短摆，永不
参与撑地）；③ **攻击抓取**（GrabTarget）→ 沿「胸→目标」满伸（可及半径钳制），观测
`HandsOnTarget[i]` = 手到目标真实距离 < GrabContactRadius；④ **爬行撑地**（§1.6）；
⑤ 走/跑混合：

```
swing = clamp(dot(对侧腿脚端 − 髋, Facing) / 腿长, −1, 1)     // 右臂随左腿
walkT = 胸侧下方垂位 + Facing·(0.08 + swing·0.25)            // 摆动读对侧腿相位：
runT  = 胸 + Facing·(臂长·0.8) + 侧向 0.22 + 上抬 0.05        //   零新增状态、天然反相、
HuntPos = lerp(walkT, runT, r)；HuntSpeed ×lerp(0.25, 1, r)   //   随真实步频自适应
```

⑥ 闲置→慢速垂到身侧（Humanoid 闲置分支原样——驼背垂手常态剪影）。
不搬 knuckle-walk 机器（KnucklePos/俯仰泵/扇扫链）——本怪直立时手从不撑地。

### 1.5 断肢（`Sever`，固定断肘/膝——标志位路线，不换实例）

Daddy 的换实例路线是因为触手段链的段数是构造自由度；本怪四肢各是**单粒子**，断肢 =
标志位 + 有效长度减半，粒子保留（哈希折叠形状不随断肢变、渲染画到粒子即肘/膝、
Shift/插值不断链）：

- **臂断**：`Severed=true + ForceRelease`——此后链只给垂落，粒子即残肢肘端
  （渲染/命中直接消费）。走路不受影响（臂本不承力，smoke 断言里程偏差 0.0%）。
- **腿断**：`ForceRelease + JointDist×0.5 + 停用步态机`（此后控制器手动积分残肢膝端
  粒子：阻尼 0.85 + 垂位弹簧 + 重力 + 距髋钳制 + SpherePenetration 出地形）——半长 Limb
  若继续走步态机，FindGrip 落点计抓地会污染推进计数。**存活腿 `Pair = null`**：
  Lookahead 释放 guard 的 `Pair.GripCounter > 0` 会被断腿的冻结计数卡死，存活腿每步
  拖到 1.75× 失效阀才松（`Limb.cs` 的 `Pair is null ||` 短路是现成逃生口，设计期核实）。
- **摔倒涌现**：`ApplyGravity = !(Conscious && Grounded && CanStand)`——断任一腿即重力
  回归、直立伺服静默，身体自然倒下。零过渡状态机、零计时器（实测断腿后 6~7 tick 内
  胸高跌破站高 55%）。
- 返回 `RatFiendSeveredLimbState`（末端粒子 Pos/LastPos/Vel 原样交接——断落瞬间渲染
  插值无缝，Daddy 先例）；已断再调 throw；昏迷可断；`staggerImpulse` opt-in 默认零
  （回归路线不传 → 基线不受影响）；`SeverSerial` 单调事件序号（渲染抽搐按沿触发）。
  单向不可逆（不做接肢；重开 = 宿主重建控制器）。

### 1.6 爬行（Crawling = 清醒且断了任一条腿）

- **重力常开**，躯干贴地靠 chunk 地形碰撞 + SurfaceFriction（Vulture 栖息爬行体制），
  零悬浮伺服；有支撑时微小抬鼻力偶（吻部离地）。
- **手臂撑地 = 控制器层 plant-and-trail 子链驱动哑粒子 RatArm**（Humanoid 手撑地的
  既有形态，不给臂造 Limb）：入场门 = `Grounded`（摔倒还在空中不许抢先扎手，零额外
  射线）；找点 = 胸前 `EffectiveLength·0.55` 处竖直下探（tick 奇偶交替，每 tick ≤1 根）；
  锁定后身体从旁爬过 = trail；合法窗（身后 0.25 / 身前 1.1× / 距离 1.4×有效臂长）超限重找。
- **存活腿**照常 `leg.Tick`——髋贴地时 FindGrip 自动退化成短促蹬地，`Gripping` 计入。
- **推进 ∝ 抓地肢体数（涌现核心）**：`force = CrawlForcePerLimb(0.011) × GripCount ×
  RunSpeed`，胸髋各按 CrawlMaxSpeed(0.045) 余量钳制。抓地计数 = 撑稳的手（有撑点 +
  吸附/够近 + 地形接触）+ 抓稳的存活腿，读上一 tick 收尾状态（固定序确定性滞后）。
  消融开关 `EnableCrawlGripScaling=false`（引擎改常数）→ 断肢里程单调必塌（smoke 红灯：
  3/2/1 肢 = 23.2/20.4/10.4m → 消融后全部 24.1m）。
- **调头两件套（R8 修复轮）**：目标在身后时——① `Facing` 限速回转
  （`CrawlTurnRatePerTick` 0.06 rad/tick ≈ 180°/1.3s，反平行退化绕重力上轴；直立自
  R13 起同走限速回转，速率 0.22——见 §5.10）；② 胸沿 Facing 推、**髋沿「追胸」的拖车轴
  推**（胸−髋投影到地面）——胸髋同向推绕不出转矩，Facing 转了身体轴还倒着。直线爬行
  两方向都收敛等于 effMove，逐位不变；调头时身体被胸拖着画弧转身、头绕外侧扫过。
- **爬台阶 = 手拉体升（R9 修复轮）**：撑稳的手高出 chunk 底面（`CrawlClimbDeadzone`
  0.04m 死区外）→ 该 chunk 竖直速度朝 `CrawlClimbMaxRise`(0.035) 伺服，注入 ≤
  `CrawlClimbGain`(0.5) × 高差（临顶自然减力）；chunk 底面追平撑点标高即熄火——身体
  正好落上台面，自终止。同时**豁免滑墙**：正对台阶立面时 `SlideAlongWalls` 会把意图
  消成零（头对头 slid 长度归零），推进与拽升双双熄火，所以「正在攀台阶」（撑点高出
  胸底死区外）期间不滑墙。平地撑点与 chunk 底面同高 → 两者恒不触发，**平地爬行
  （含调头）逐位不变**。可爬高度无显式参数：高过「探针举高 + 胸高」的台面探不到
  （起点陷入固体 → 零法线拒收），涌现上限 ≈ `CrawlProbeRise`(0.3) + 胸径 ≈ 0.5m，
  更高的立面自然回到「墙」语义（滑墙绕行）。
- **四肢全断蠕动**：`GripCount==0 且有移动意图` → 周期性内力偶
  `WeightedPush(胸, 髋, Facing·0.002·sin(2π·tick/60))`——动量守恒内力靠摩擦整流，
  近零位移的挣扎感（smoke：2000 tick 位移 0.15m < 0.5m 上界）。

### 1.7 攻击接缝（最小面——玩法 100% 宿主侧，Daddy 竞技场惯例）

刻意不做 TentaclePlant/Daddy 式的内核抓取通道（触手物理才需要 Reached/Held/
PositionCorrection 全套）；接缝只有：输入 `GrabTarget`（Vector3?，逐 tick 写/清）+
`LookTarget`（Vector3?，凝视 = 猎杀姿态，opt-in 默认 null，见 §5.12——R19 吃痛回神窗的
「头照瞪手照举」也建立在它上）+
`MouthDrive`（0..1）+ `GrabHandSpread`（R19 抓取双手横向分开，opt-in 默认 0——两手
默认追同一点逐位重合，渲染两套手爪叠置 z-fighting 读成「手在颤抖」）+
`Impact(chunk, Δv)`（R18 部位冲击，opt-in——单 chunk 速度注入，与 Launch 的区别是
幅度小且不碰肢体/撑地，踉跄退步由伺服 + plant-and-trail 涌现）+ `ImpactTwist(yaw, up)`
（R19 朝向拧转，opt-in——绕 up 拧 Facing，侧向命中的躯干拧身；恢复靠既有 SlewFacing
涌现）；观测 `HandsOnTarget[2]` + `MouthOpen/LastMouthOpen` + 手粒子位置。宿主自己判定双手
到位、计时咬合（MouthDrive 脉冲 + 自己数咬合次数）、束缚站位（R18 起 = 锁镜头窗内
把玩家指数收敛到怪物正面定距——锁输入态玩家水平自驱为零，分步修正不会被抵消；
TentaclePlant「全量交付」教训只约束自驱活跃的目标）、放开与冷却；死亡瘫倒走既有
`Conscious=false`（伺服全停 + 重力回归，倒地涌现），定格后宿主停 Tick 即「完全固定」。
头朝向在 GrabTarget 非空时由内核自动转向目标；咬合前突是渲染层化妆。

### 1.8 确定性折叠（`FoldState`，DropBug 模板）

FoldBody → Facing → Gait → CrawlFactor → GroundedCounter → SeverSerial → 4×severed →
逐腿（Pos/Vel/ReachingForTerrain/HasGrip/GripCounter）→ 逐臂（Pos/Vel/Mode/GrabPos/
ReachedSnapPosition/TerrainContact/Severed）。MouthOpen/HandsOnTarget/CrawlGripCount
是派生量不折叠；Last* 是派生历史不折叠。普通路线从不调 Sever → 两族基线正交。

### 1.9 显式家具翻越（宿主选路，内核只执行）

`TraversalIntent = RatTraversalIntent(Phase, Target)` 优先于 `MoveTarget/MoveDir`，阶段固定为
`Approach → MountAndCross → Stabilize`；`AtTraversalTarget` 只报告当前阶段完成，路线选择、
概率、失败冷却和阶段推进全部归宿主。Mount 的 Target.XZ 是顶面内的远侧引导点，Target.Y
是顶面标高：该阶段豁免墙滑；普通 `HipServo` 暂停，`Body.Tick` 和真实碰撞仍是唯一位置权威。

**Mount 期身体动力（R22 手撑桌面，见 §5.18）**：双手 plant-and-trail 撑顶面（探针几何
复用 CrawlProbe*、从「顶面标高 + CrawlProbeRise」竖直下探、命中低于顶面
`MountSurfaceTolerance` 拒收——手只许撑桌面不许撑地板；未撑住时朝本手一侧的桌面落点
前伸，两手横向分开）。拽升 = `HaulChunkUp` × (`MountHaulBase` + `MountHaulPerHand` ×
撑稳手数)；未越顶的前向推进油门（注入+天花板同乘）= `MountDriveBase` +
`MountDrivePerHand` × 撑稳手数（钳 1，双手 = 满速）——**翻越能力 ∝ 撑稳手数**。
撑稳判定与爬行同一 `HandPlanted` 三条件，计数走只读观测 `PlantedHandCount`。

普通前探新增 `WalkStepMaxRise=0.35m`：高于脚下真实支撑面的家具不再被髋伺服误当成普通
台阶；0.3m 门槛照旧可走，桌台必须由显式意图授权。`TraversalIntent=null` 时旧路线哈希逐位
不变。Shift 平移 Target；Teleport/Launch 清意图。宿主能力快照至少保留一只手才可下发
（R22 后有物理背书：断臂翻越显著变慢，1.05m 高桌 Mount 段双手 18 / 单手 24 / 双臂全断
50 tick）；一臂执行与手数单调性均由 smoke 覆盖。

## 2. 品种预设

| ID | 定位 | 关键差异 |
|----|------|---------|
| `ratfiend/gaunt` | 基线：修长驼背苍白鬼怪 | 表内默认值；HunchAngle 25° |
| `ratfiend/dusk` | gaunt 的**暗剪影调色变体** | 体格参数逐位相同只换 ID（调色板归渲染层）；矩阵断言 dusk/walk 哈希与 gaunt **逐位相等** |
| `ratfiend/broad` | 重型 | 大体格短脖、HunchAngle 22°、腿慢步大、爬行力略低 |
| `ratfiend/whelp` | 幼体 | 小体格快节奏、HunchAngle 28° 驼得更深 |

## 3. 正式渲染（`RatFiendFormalRenderer`，渲染件七号——首个可动颌）

- **头 = 颅吻一体变径扫管**（5 站楔形颅→细长吻，鼻尖渐暗）。**刻意不用共享 SphereMesh
  头**：`TubeMeshBuilder` 顶点色烘焙包裹漫反射（ShadeAmount 0.22），与平色球在苍白色下
  会在拼缝处露 ~20% 亮度断层（近黑的 Humanoid 头看不见这个坑——设计期新发现）。
- **可动下颌**：绕耳下铰点旋转的 3 站短管，张角 = `jawRest(3~8°) + (gape(45~55°) −
  rest)·mouthSmoothed`；口腔 = 沿两颌角平分线的暗红喉管，半径 ×clamp(mouth·1.6)
  闭嘴缩进颌内。**抗抖四件套**：头帧每帧只算一次、张角单独低通（开 25/合 18 非对称）、
  牙 = (头帧, 张角) 纯函数零累积、jawRest ≥3° 永不真正咬死。渲染 target 用
  `lerp(LastMouthOpen, MouthOpen, alpha)` 插值 + 附加化妆（闲置微呼吸颌/跑步喘/
  挣扎加张/断肢尖叫）。
- **牙**：脏白 AddBlade 沿上吻下缘/下颌上缘两条唇线（上 5~8/下 4~7 每侧、12% 缺牙、
  前犬齿 ×1.2~1.6，seed 冻结），根色暗牙龈、上下错半齿位防闭合 z-fight。
  **有意偏离 Humanoid 的「头色颅骨锯齿」哲学**——本怪是真嘴，恐怖来自嘴。
- **躯干**：七点脊柱**长弧**（颈根→肩坡→驼峰弧顶→中背→掐腰→髋→尾基），隆起摊在
  肩坡→弧顶→中背三点、沿躯干轴单调推进（负 lerp 因子 = 越过胸向头侧延伸），弧顶仍是
  全身最宽（0.78×胸径）+ 掐腰 + 椎骨凸 AddKnob；躯干管终点即脖管起点（同点同半径，
  脖根再沿脖向内埋 0.12×胸径藏缝）；呼吸（Lizard 门控，爬行时频率 ×2.7 幅度 ×0.6
  急促浅喘）；**爬行 dorsal 双源 slerp**（站姿 −facing 系 / 爬行 up 系按 CrawlFactor 混 +
  低通 k=6）——防脊轴转水平后驼峰方向乱转。
- **修复轮 R12（背部「肉球 + 凹陷」，用户实测报告）**：旧版驼峰是**单点极宽剖面**
  （0.90×胸径），紧贴其上 0.2×胸径处的颈基半径却砍半（0.38×）——半径梯度 ≈2.4×，
  管端近半球盖，背读成「背了颗肉球」；且控制点序列在胸处**折返**（颈基→驼峰→胸是回头
  路径），背侧剪影在驼峰后方塌出一道凹槽；加上脖管与躯干管在驼峰顶各自收口、端点与
  粗细都不重合，接缝读成第二处凹陷。修复 = 上一条的七点单调长弧（半径梯度全程 ≲0.7×）+
  躯干/脖管同点同半径交棒 + 臂根多埋一段（上躯干细了一档防露缝）+ 椎骨撒点区间对准
  新弧顶（0.14~0.52）。走/跑/broad/爬行四姿态截图验证：背读成弓、两处凹陷消失，
  爬行时弧顶朝上椎骨凸沿脊排列正常。
- **四肢**：TwoBoneIk 肘膝，pole 三态混合（垂手肘朝后外/前伸肘外压/**爬行肘朝上**）
  逐臂低通；钩爪 = 逐指两枚刀片折钩（seed 指长/弯度，撑地时向抓面摊开）。
  R14 起整条肢体是**一根样条管扫到底**：埋进躯干/骨盆内部的肥根锥（三角肌 0.38×胸径 /
  臀肌 0.52×髋径）→ 关节 → 微弓中段 → 末端，肉感全靠半径剖面（大腿肉吃 `_kneeBulge`
  基因、膝收骨感窄点、小腿肚微凸、掌/脚掌并进管内）。**断肢** = 同款埋根→内核残肢粒子
  的短残段（真粒子，插值无缝）+ 暗红断口瘤 + 碎肉小刀片（seed 角度）。
- **修复轮 R14（四肢「几何拼接感」，用户实测报告）**：四肢初版是 Humanoid 渲染件的
  克隆——细棍臂（根半径 0.045 从半径 0.13 的躯干表面出发）、单点膝球（2.2~2.6× 鼓包）、
  独立脚板管（踝处与腿管半径跳 3×）、手大瘤球（0.06 球粘 0.017 细腕）。Humanoid 是
  **近黑剪影**，这些管-管拼缝全部消失在剪影里；鼠煞是**苍白皮**，每条交线、每个关节球
  都露（同一教训的球头版已记在头部条目）。修复走 Lizard 路线：根点埋进躯干内部 +
  肥根快收锥（出膛角变浅，剪影里肢体「长出来」而不是「怼上去」）、`_kneeBulge` 基因
  改驱大腿肉、两骨中段沿 pole 微弓打散直圆柱感、掌/脚掌并入肢管一根扫到底、手瘤删除、
  肢管截面 6→10 边（肥根下六边形棱线会露）。肩/肘/髋/膝**位置**与 `RatFiendJointMath`
  逐字同源未动（命中胶囊零漂移），纯管剖面改动。走/跑/爬行/broad/断臂断腿五姿态
  截图对 R12 同机位验证。
- **尾**：渲染侧 verlet 短锥链（3~4 节，环纹交替明暗 = 裸鼠尾）。**修复轮 R3**：
  尾根方向初版直接用 dorsal——爬行时 dorsal = 世界上，尾巴直挺挺戳向天（首轮截图
  实测）；改按 CrawlFactor 混向水平「身后」方向 + 加重下垂。
- **动态**：咬合撕扯（触发器直读宿主咬合脚本 `MouthDrive` 的满开→全闭沿 0.75/0.25 +
  Conscious 门——R18b 定型：平滑域「下穿 0.5」在 Bite 态嘴开度地板 0.70 上永不触发、
  死亡清嘴会误触发；命中后 0.22s sin 包络的头前突 0.09m + 下压 + 高频侧向撕扯，
  R18 从 0.12s/0.02m 前突加强）、断肢抽搐（SeverSerial 沿触发 0.6s
  jitter + 残肢甩 + 嘴尖叫）、爬行挣扎（驼峰侧向蠕摆 + 瞪眼 + 急喘）、眨眼 PRNG
  （Nervous 低 → 少而慢更瘆人）、耳后折（跑步/受创）与耳颤。
- **R18 生动性轮**（用户实测四项 + 死亡配套，全渲染侧化妆、不进哈希——细节见 §5.14）：
  ① **步态起伏**：读双腿沿 Facing 的前后展开差作相位（摆臂同源手法），竖直起伏
  （双支撑最低/过腿最高，2×步频）+ 侧向摇摆，幅度走→跑升档（0.014→0.038m /
  0.010→0.026m）、爬行与昏迷归零；偏移只搬躯干系（胸/髋/头/肩/髋关节），脚手读内核
  原位——踩点不动，起伏被肢体 IK 吸收（真实步行形态；≤4cm 在命中冗余内，代理不动）。
  ② **口腔重做**：旧细长暗锥（伸到嘴前缘、近黑）远景读成嘴心「暗鼻头」；改宽根短锥
  （根缩进颅内、收口退到 0.88·headR）+ 喉底暗→口腔亮红渐变，四档 Maw 调亮到
  (0.30,0.08,0.065) 档。③ **爬行头 up 参照**：headUp 纯用 `_spineUp` 时爬行脊轴转
  水平后与 headFwd 近共线、去轴残差翻向下 → 整头绕视轴滚 180°（颌朝天）；up 参照按
  CrawlFactor 混向世界上（dorsal 双源同理）。④ **嘴部张闭起伏**：双正弦**负偏置**调制
  （只朝闭合方向脉动——张开侧被 clamp(1) 吃掉，正偏置在血盆大口档不可见），幅度随
  开度升档；与呼吸/喘分频防拍频。⑤ 死亡门控：`!Conscious` 时呼吸/耳颤/嘴起伏/步态
  起伏全部归零（尸体胸廓起伏是最出戏的假活）。
- **调色**：用户定档苍白灰皮为主（gaunt/broad/whelp；暗鼻/暗爪尖/暗眼窝拉回剪影感）；
  dusk = 近黑身 + 骨白鼠颅（Vulture 先例）。`srgbVertexColors: true`。
- **修复轮 R4（白盒标记漏显）**：白盒渲染器的目标标记球在 Draw 里按业务条件覆写
  Visible，把 SetVisible(false) 顶掉——正式视图上浮着黄/粉标记球（首轮截图实测）。
  修复：白盒记住整体可见态，标记 Visible 与其相与。

## 4. 沙盒与回归

### 4.1 回归沙盒（`scenes/ratfiend_sandbox.tscn` + `scripts/ratfiend_sandbox/`）

平地 + 0.3m 台阶（walk 环线跨越）+ 转向墙。交互键位：WASD 跑（Shift 慢走看驼背摆臂）、
**K 断掉下一条肢体**（腿→臂演示序，看三肢爬行）、R 重生复原、T/Y 设/清抓取目标、
M 嘴开合、B 击飞、V 正式/白盒、1~4 预设。长路线用**有界巡逻**（两点乒乓 MoveTarget）
——平地带宽 ±22m，恒向 MoveDir 会走出地板边坠落（设计期修复：攻击路线放开后的
行走曾走出边缘，travel 从 101m 异常值收敛到 37m）。

### 4.2 独立入口与回归

```bash
dotnet run --project core/ratfiend_smoke        # 无引擎冒烟（22 门 + 7 消融红灯）
./tools/run_ratfiend_matrix.sh [输出目录]        # Godot 矩阵 27 项 + smoke
```

**无引擎 smoke**（`core/ratfiend_smoke/Program.cs`，基线 `ExpectedHash =
0xA7C06ECCF4698B43`，R17 断臂垂位重钉；**真相源是代码常量，本节数字只是快照**）：
ASSEMBLY（拓扑/钉定/Pair 互绑/ById 抛异常/出生冻结/dusk 同构）、
DET（1400 tick 主路线含固定 tick 断腿→爬行→断臂：双跑 bit-exact + 微扰必变 +
routeCovered）、WALK（里程 41m（R11 速度分离后 0.6 油门 ≈2.1 m/s）+ 驼背几何：静立
200 tick 后 dot(胸−髋, Facing)=0.42
≥0.15 + 后半窗漂移 0.009m<0.15 + **消融红灯 ①**：EnableHunchTilt=false → 静立前倾
−0.001）、POSTURE（RunSpeed 0.2/0.5/0.8/1.0 稳态：头高 [−0.34,−0.27,−0.06,+0.04]、
嘴 [0.15,0.29,0.56,0.70]、前伸 [0.09,0.36,0.79,1.01] 三链严格单调）、ARM-SWING
（反相摆 74%≥60% + 手前后摆幅 0.235m≥0.1）、WALK-SWING（R15：walk 档腾空段
10.1 tick > 消融 6.0×1.4 + run 档 6.0<walk + 步周期 22.1 在消融 18.0 的 ±35% 窗内
（步频不塌）+ **消融红灯 ⑤**：LegSwingSpeedFactor=1 → 腾空段缩回快摆基线）、
LOOK-TARGET（R16/R16b：RunSpeed 0.35 低速走姿逐 tick 喂前方眼高凝视点 → 头轴点积
+0.30 vs 不喂 −0.70 + 手前伸量 0.83m vs 0.14m（差各 ≥0.3，**消融红灯 ⑥**：不喂 =
耷拉基线）+ Shift 平移凝视点 + Teleport 作废；哈希中立由 DET 门背书——回归路线从不
设置 LookTarget）、
SEVER-API（播种逐位连续/叠断 throw/昏迷可断/stagger 精确）、
SEVER-ARM-WALK（断臂行走偏差 0.0%<5%）、ARM-STUMP（R17：双断臂站立稳态残肢各偏本
肩侧 0.134m≥0.08 + 双残端相距 0.267m≥0.18 + 垂在胸下 −0.56m<−0.3；**消融红灯 ⑦**：
去掉断臂分支的肩侧垂位弹簧 → side/split 塌回 ≈0 中线收敛）、
SEVER-LEG-CRAWL（7 tick 摔倒 + 爬行 34.9m +
存活腿步频 34 循环 + Pair 已清）、CRAWL-TURN（爬稳后目标反向：Facing 每 tick 回转
≤0.06 rad + 48 tick 收敛 + 回程 25m + 头髋水平距全程 ≥0.32m（实测 0.93）+ 身体轴
真的转过来 endSpineAlign=1.00 + **消融红灯 ③**：限速调成 π/tick（=旧瞬时置向）→
头髋最小距塌到 0.048m 穿插带）、CRAWL-STEP（断腿爬行撞 0.3m 台阶：翻上后继续爬
（endX=27.3 > 台阶沿+3）+ 胸落台面（Y=0.51）+ 直线不被滑墙滑歪（zDrift=0）+
**消融红灯 ④**：CrawlClimbGain=0（撤手拉体升）→ 卡死在台阶面前 X=2.79）、
SEVER-MONOTONE（3/2/1 肢里程严格递减 + **消融红灯 ②**：
EnableCrawlGripScaling=false → 极差塌为 0）、SEVER-ALL（蠕动 0.15m<0.5 不逃逸）、
ATTACK（2 tick 双手到位/满嘴/放开归零/不可及永不误报）、GRAB-SPREAD（R19：spread=0
双手逐位重合 sep=0.0（z-fighting 根因钉成基线事实）+ 抓住判定成立；spread=0.09 分开
0.180m ∈[0.15,0.21] 且判定仍双双成立）、IMPACT-TWIST（R19：拧 16° 与 Rotated 期望点积
>0.9999 + 无意图 10 tick 保持拧姿 + 意图恢复后 SlewFacing ≤3 tick 甩回 + 零轴退化安全 +
连拧 200 次单位长度守恒）、LIFECYCLE（Shift 逐字段精确含
断肢态/Teleport 作废/Launch 恢复）、QUERY（rays max 21≤40、shapes max 7≤8）、
PENETRATION（残余穿透 0 <2mm）。

**Godot 矩阵 27 项**（`tools/run_ratfiend_matrix.sh`，哈希 2026-08-22/23 实跑钉定；
R8 调头修复重钉 sever-leg / sever-both-legs / broad- / whelp-sever-leg 四条——巡逻
乒乓折返落在爬行段，正是调头行为改变的路线；R9 新增 crawl-step；R10 站姿、
**R11 步态修复、R13 直立调头各整表重钉一次**（速度/步态/Facing 进哈希；R13 后纯爬行
路线四条与 R11 逐位相同，见 §5.10），流程均为行为门先全绿再钉值）：
walk-a/b（双跑 diff）、walk-40（40Hz 时基不变）、perturb（1mm 必变）、run（跑步姿态：
maxRunBlend 1.0 + 跑步窗嘴均值 0.70≥0.6）、yank（击飞 621 tick 落地恢复，累计续走
45m——R11 起度量为累计里程，见 §5.9）、
sever-leg（摔倒 507 + 爬行 33.4m 含一次乒乓调头 + 抓地 3）、crawl-step（断腿爬行翻
0.3m 台阶到台面目标点：摔倒 506 + 抓地 3 + 到点 + 末态胸 Y≥0.35 落在台面——内核
CRAWL-STEP 门的引擎侧对照，验证 Godot 射线 HitFromInside 语义一致）、sever-arm-walk（断臂巡逻
86m 不受影响）、sever-both-legs（双断腿纯臂爬 27.5m 含调头）、sever-all（全断漂移
0.16m 蠕动不逃逸）、
attack（喂目标后 2 tick 双手到位 + 咬合脉冲满嘴 + 放开）、sever-during-attack
（攻击中断执行臂 → 断臂手的「抓住」观测立即消失——竞技场并发规则的内核侧保证）、
dusk/broad/whelp 变体（walk + 断腿爬行，变体只跑 walk 会漏检断肢——DropBug 评审 P2
同款教训）+ 交叉红灯：double-run / 40-vs-400 / perturb / preset-difference /
**dusk-parity**（dusk 哈希必须与 gaunt 逐位相等——体格同构的免费断言）。
另含 traversal-a/b、traversal-40、traversal-perturb：0.82m 实体桌必须完成三阶段、越过
出口平面、连续支撑 8 tick、残余穿透 <1cm、单 tick 粒子位移 <0.25m；宿主阶段与稳定计数
一并折入新路线哈希，双跑与 40/400Hz 逐行一致，旧 23 条路线不重钉。

演示专用路线 `traversal-loop`（**不进矩阵**，R22 随行）：桌两侧往返翻越（目标按桌心
镜像），每趟落地站 1.2s 再折返——手撑窗口只有 ~0.5s、一次性路线肉眼极易错过，循环版
让它每 ~3s 重现一次。**窗口模式无需 `--ratfiend-determinism` 即自跑**（WASD 被意图接管，
相机/K/R/1-4 热键照常）；确定性模式下自检 loops≥2 + planted=2：

```bash
/Applications/Godot_mono.app/Contents/MacOS/Godot --path . --log-file /private/tmp/godot_codex.log \
  scenes/ratfiend_sandbox.tscn -- --ratfiend-route=traversal-loop [--ratfiend-tps=20 慢放]
```

**交互模式自动翻越**（R22 随行，仅窗口 WASD 模式，不进任何确定性路线）：手撑由宿主喂的
MountAndCross 意图触发，手动驾驶原本没人喂意图 → 撞桌只有普通楔升 + 举手跑姿。沙盒现内建
主仓 `RatFiendTraversalExecutor` 的极简替身：驾驶方向 0.9m 内探到竖直立面、其后顶面高出
脚下 0.35~1.15m（普通高抬步之外、主仓 CrossObstacle MaxRise 之内）即合成 Mount 意图
（0.3m 台阶实证不触发），内核接管导向并撑手；到点 / 转向背离 / 4s 看门狗撤销还给 WASD。
HUD 加 `vault on planted=N` 观测。另有 `--ratfiend-drive=+x|-x|+z|-z` 恒向驾驶钩子
（解放键盘 + 跟拍相机，验证截图即用它）。

### 4.3 枪击竞技场（探索场景，不进矩阵）

`scenes/rat_arena.tscn` + `scripts/rat_sandbox/`：第一人称玩家 + 枪 + HUD。
命中判定 = 宿主侧纯数学（`RayHitMath` 射线 vs 球/胶囊，从 Daddy 竞技场提取共享）对
13 个部位代理（头球/躯干三件/每肢两段胶囊，肘膝点用 `RatFiendJointMath` 与渲染件同源
公式），最近 t 即部位；命中臂/腿 → 扣逐肢血量（`LimbHp` 2，R20），打空才 `Sever`
固定断肘/膝 + 断落物（`RatSeveredPiece` 3 点 Verlet，Daddy SeveredPiece 砍 Traction
的两态版）。

相位机（R18 重构，R19 收敛）：`Chase / Strike / Grabbed / Bite / Dead` 五相位；攻击冷却
= `_attackReadyTick` 时间戳（旧 Recover 相位并入 Chase 后删除，R18），硬直 =
`_stunUntilTick`（封攻击门）+ `_reelUntilTick`（吃痛回神窗）两个时间戳（旧 Stunned
相位删除，R19——硬直本质是「带门禁的 Chase」，姿态保持凶相、追逐照常）。玩法闭环：
- **追逐**恒满油门（近身减速机制取消——它的初衷「防止抓住时站位过近/过远」改由束缚期
  完成：锁镜头窗 `CameraTakeoverSeconds` 内把玩家指数收敛到怪物正面水平
  `GrabHoldDistance`（0.9m，爬行 ×0.85），只修水平、竖直交给玩家自驱重力）。
- **攻击门按存活手数选路**：≥1 手 → Strike 扑抓（双手到位 = Grabbed 束缚 + 咬合，
  双手横向分开 `GrabHandSpread` 0.09m 掐住两肩）；零手 → Bite 无束缚撕咬（边追边咬，
  闭口 tick 玩家在头前 `BiteReach` 内才咬中——蓄势期跑开可躲）。门距 = 姿态基准
  （站立 `AttackStartRange` 1.8m ↔ 爬行档，按 CrawlFactor 连续混合——站立扑抓靠
  3.4m/s 冲刺动量滑进手可及圈 1.40m 补死区，爬行推进弱且扑抓期双手离地推进即断，
  门必须收进可及圈，R19）× 存活手数缩放（2/1/0 手 = 1 / 0.94 / 0.88——用户定调
  「打断双手才有明显收益，单断不明显但不能没有」）。爬行档 = min(`CrawlAttackRange`
  1.3m, 按**预设臂长**现算的可及上限 (臂长+接触余量)×0.95)（R19b——whelp 臂长 0.85
  下固定 1.3 会让死区复发）；零手 Bite 档不钳（落咬由 BiteReach 判）。追逐停靠半径
  钳到门内侧 ×0.85（R19b——停靠是水平距、门是 3D 距，零手爬行档门 1.144m 小于
  停靠点 3D 距 1.21m，怪物会在门外 6cm 永久停车瞪人）。
- **咬合脚本语义翻转**（Grabbed/Bite 共用）：蓄势窗血盆大口（MouthDrive 1）→ 闭口窗
  （0）= 咬中沿——渲染撕咬顿挫挂在 1→0 沿上，与「咬中」结算同 tick（旧脚本反着张、
  顿挫被推迟到放人后，「咬得不明显」的根因）。
- **爬行攻击瞄点**：`PlayerAimPoint()` 按 CrawlFactor 从眼高（1.55m）连续降到小腿高
  （`CrawlAimHeight` 0.45m）——爬行胸高 ~0.3m + 臂长 1.15m 够不到眼点，旧版爬行扑抓
  必然超时空放；降瞄后爬行抓咬与站立同链（束缚镜头对准怪物头，pitch 自动朝下）。
- **枪械反馈链**（HandleHit 固定序：伤害 → 击退 → 肢体血量/断肢 → 死亡/硬直）：血量
  `MonsterHp` 10、头部 ×3（四枪爆头毙命）；臂/腿另有逐肢血量 `LimbHp` 2——打空才断肢、
  一枪打不断（R20 用户定调），扣肢体血与扣总血同发并行（断完四肢正好 8 发，躯干再补
  两枪毙命），HUD 四格显示各肢剩余发数；部位击退走内核 `Impact`/`ImpactTwist` 接缝、
  方向按部位塑形（R19）——头 = 沿「枪向 + 向上偏置 `HitImpactHeadUpBias`」甩头带仰角
  + 胸 0.5× 水平退身，躯干 = 胸满额 + 髋 0.7×，四肢 = 冲量转注相邻躯干 chunk（手脚是
  追目标粒子，直接推会被伺服吃掉）+ 躯干拧转（转向 = 力臂×枪向 转矩符号：打右臂右肩
  后拧，臂 16°/腿 9°）；硬直 `Stun{Head,Body,Limb}Seconds`（0.8/0.35/0.5s，重复命中取
  max）只封攻击判定门（贴脸走过也不被抓）——姿态保持凶相、追逐照常（R19 用户定调），
  前缀 `StunReelSeconds`（0.25s）吃痛回神窗内掐移动意图：击退/拧身不被推进与朝向回拽
  掩掉、定住瞪人（LookTarget 照钉 → 头照瞪手照举），窗尽 SlewFacing 把拧开的朝向甩回
  猎物；HP 归零 → `Conscious=false` 倒地涌现，全粒子静止 20 tick 后**尸体永久冻结**
  （停 Tick，零物理零交互，再打只回 toast）。断臂并发钉子（攻击占用臂被断 → 放人 +
  完整冷却）独立于硬直存在——硬直秒数调零时不变量仍成立。
调参全 [Export]（Daddy 纪律）；断腿后爬行追逐是内核涌现，宿主不设 Downed 相位。

追杀观感（R16 定型、R18 更新）：① 瞄准点**四处同点原则**（攻击门/GrabTarget/脱围
距离/束缚修正共用 `PlayerAimPoint()`——瞄哪、抓哪、锁哪必须一致）；点位站立 =
玩家眼高 `EyePosition` 1.55m（= 镜头挂点，此前用胶囊球心 0.85m 是「往脚下抓」），
R18 起按 CrawlFactor 连续降到小腿高（见上文爬行瞄点条目）。② 追逐期逐 tick 喂
`LookTarget` = 同一瞄准点——头不耷拉、手前伸备抓（内核 LookArmRaise，见 §5.12）。
③ `ChaseMouthDrive`（[Export] 默认 0.6）在 Chase/Strike/Bite 蓄势外常开龇嘴；束缚/
撕咬的嘴由咬合脚本独占（蓄势窗拉满 1.0 → 闭口窗 0，R18 语义翻转见上）。
Strike/Grabbed 期 LookTarget 置 null，头朝向由 GrabTarget 独占。

## 5. 有意偏离与设计裁定清单

1. 驼背 = 显式倾斜姿态轴 + 天花板差分，**不是** Humanoid 的阻尼差路线（§1.2 两轮修复）。
2. 断肢 = 标志位 + 长度减半，**不是** Daddy 的换实例（单粒子肢体没有段数自由度）。
3. 攻击接缝 = 最小面（GrabTarget/MouthDrive/HandsOnTarget），**不是** TargetSnapshot/
   TargetEffect 全套（玩法 100% 宿主侧）。
4. 牙 = 脏白真牙 + 可开闭嘴，**有意偏离** Humanoid「不是嘴」的颅骨锯齿哲学。
5. 走/跑 = RunSpeed 连续油门 + Gait 平滑标量派生姿态，**无 gait 枚举**（涌现惯例）。
6. MouthOpen 纯派生不进哈希；Gait/CrawlFactor 进哈希（物理量目标点的平滑源）。

## 5.5 对抗性评审轮（2026-08-22：四分区并行评审 + 逐条对抗核实，7 初判 → 3 核实）

| # | 核实为真 | 修复 |
|---|---------|------|
| R5（high，假绿） | smoke LIFECYCLE 的 `Shift(+512)` 把身体挪出 200m 有限盒地板——推进无 Grounded 门，失重巡航照常累积里程，「续走/击飞恢复」两个门靠**空中漂移**通过，没测到落地恢复 | 该检查改用无限半空间地板；两门补 `Grounded && !ApplyGravity` 条件（空中漂移不再冒充续走） |
| R6（low） | `RatFiendJointMath.Dorsal` 的 Slerp 在两源反平行（摔倒翻滚瞬间脊轴倒置 + 三向量近共面）时退化返回零向量/噪声方向——文件里唯一没有退化守卫的混合 | 混合结果加 LengthSquared 守卫，退化回退站姿源（误差本被 0.22m 瞄准冗余覆盖，属鲁棒性缺口） |
| R7（low） | 竞技场断落物 X/Z 钳位面取内径名义平面，但四墙墙心骑在该平面上、向房内突出半墙厚 0.09m——断肢可静止在墙体内、视觉半埋（Daddy SeveredPiece 模板继承的既有偏差） | 钳位面各内缩半墙厚到墙体内表面（地板/天花板走面恰在名义平面，不缩） |

被驳斥的 4 条（核实代理逐条给出反证）：爬行里程用路径长非位移（推进沿意图方向，塌缩场景不成立）、
确定性退出的帧内多步竞态（误读 Godot Quit 语义）、Sever 后 HandsOnTarget 单帧残留
（窗口存在但所有宿主的消费时序都不落入）、CLAUDE.md §6.4 文字过期（本轮已同步更新）。

## 5.6 R8 爬行调头修复轮（2026-08-22，用户实测报告）

**症状**：爬行时目标在身后 → 头直接从裆下穿过去，手在头后、脚在头前，头髋穿插——
身体从不整体转向。此前该路径**没有任何断言覆盖**（smoke/矩阵的爬行全是直线段），
bug 因此存活到交互实测。

**根因是两层，缺一不可（初判被推翻一次）**：
1. `Facing = effMove` 瞬时置向——站立时有腿 + 站立力偶把身体真转过来，问题被掩盖；
   爬行时身体是贴地水平链，Facing 一帧翻 180°，头伺服/推进/手撑点全部立刻反向。
2. **初判只修 ①**（Facing 限速回转）后实测头髋最小距仍 0.050m ≈ 消融值 0.048m——
   第二层根因：推进对胸、髋沿同一方向推，**绕不出转矩**，Facing 转过去了胸髋杆还倒着，
   等效「倒着爬 + 头被拽过裆」。

**修复**（`RatFiendLocomotionController`，全部物种局部）：爬行时 ① Facing 限速回转
（新出生参数 `CrawlTurnRatePerTick` 0.06 rad/tick，反平行退化绕重力上轴；站立不受限）；
② 髋的推进方向改「追胸」拖车轴（胸−髋投影到地面）——杆被胸拖着自然回转。
两者在直线爬行时都收敛等于 effMove，**直线基线逐位不变**（SEVER-LEG-CRAWL/
SEVER-MONOTONE 数值一字不差）；主 DET 路线因摔倒过渡段脊轴未对齐而漂移，有意重钉。

**验证**：新 smoke 门 CRAWL-TURN（回转限速 0.0600 恰等上限 / 48 tick 收敛 / 头髋
最小距 0.94m ≥0.32 / endSpineAlign=1.00 / 消融红灯 π/tick → 0.048m）；矩阵重钉 4 条
爬行折返路线（sever-leg / sever-both-legs / broad- / whelp-sever-leg），其余 18 项
逐位不变；sever-both-legs tick 970→1060 连拍目检：拉弧转身、头绕外侧、无穿插。

## 5.7 R9 爬行翻台阶修复轮（2026-08-22，用户实测报告）

**症状**：断腿爬行连沙盒里 0.3m 的小台阶都上不去——实际放进游戏会被随便什么地形卡死。
（走路态过台阶没问题：腿的 plant-and-trail 落脚 + 髋高伺服天然会踩上去。）

**根因是两层耦合**：
1. **爬行推进纯水平**：胸球半径 0.21 < 台面 0.3，球心低于台阶沿，SpherePenetration 对
   立面的排斥是水平的——推进永远顶墙。手探针（起点 ≈ 胸高+`CrawlProbeRise` 0.3 ≈ 0.51 >
   台面）**早就能把手搭上台面**，但撑点只进抓地计数，没有任何「把躯干拽上去」的力。
2. **滑墙把意图消成零**：台阶立面对爬行躯干是「墙」，正对时 `SlideAlongWalls` 的
   slid 向量长度归零 → effMove=0 → 推进与（若有的）拽升双双熄火，彻底钉死。

**修复**（全部鼠煞局部）：① **手拉体升**——撑稳的手高出 chunk 底面（0.04 死区外）时
把该 chunk 竖直速度朝 `CrawlClimbMaxRise` 伺服（注入 ≤ `CrawlClimbGain`×高差，
底面追平撑点即自终止，身体正好落上台面）；② 攀台阶期间（同一判据）**豁免滑墙**。
撑稳判据与抓地计数共用同一 `HandPlanted`（数进引擎的手才有资格拽人）。平地撑点与
底面同高 → 两者恒不触发：**全部既有基线（smoke DET + 矩阵 22 项）逐位未动，零重钉**。
可爬高度是涌现上限（≈`CrawlProbeRise`+胸径≈0.5m）：更高的台面探针起点陷入固体、
零法线拒收，自动回到「墙」语义滑墙绕行——不需要显式高度参数。

**验证**：新 smoke 门 CRAWL-STEP（翻上 0.3m 台阶后续爬 24m / 胸 Y=0.51 落台面 /
零侧偏 / 消融 CrawlClimbGain=0 → 卡死在 X=2.79 台阶面前）；矩阵新增 crawl-step 路线
（断腿爬行到台面上的目标点，哈希 496782DEC1DB8EDB 首跑钉定）；其余 22 项全绿未动。

## 5.8 R10 站姿修复轮（2026-08-22，用户实测报告）

**症状**：常态站立/行走是「蹲马步」——膝深弯、站距外撇，不符合人形生物的物理常识。

**根因是三个数字的合谋**（单看任何一个都不致命）：
1. `HipRideHeight 0.45` 只有腿长 0.75 的 **0.6 倍**（注释还写着「给渲染 IK 留屈膝余量」
   ——余量留成了主姿态）；
2. 渲染腿骨 `JointDist×0.60×_legGene`，双骨合计 **1.2~1.5 倍腿长**——把 ≈0.5m 的髋足距
   塞进 0.9~1.13m 的骨链，两骨 IK 膝内角只剩 ≈67°（几何上就是深蹲）；
3. `LegLateral 0.3`（方向系权重）把落点外撇 ≈0.22m——马步的横向开档。

**修复**（内核参数 + 渲染系数联动，三档预设同步）：
- 内核：`HipRideHeight` 抬到 **≈0.83×腿长**（gaunt 0.66/0.8，broad 0.7/0.9，
  whelp 0.47/0.58）；`LegLateral` 0.3→0.14（脚落髋正下）；`FeetDown` 0.6→0.75
  （broad 0.85）。**联动硬约束**：`Limb.FindGrip` 的 goal 高度 =
  站高 − 腿长×(0.3×FeetDown/|dir|) 必须 ≤ 0.55（探针下扫深度），否则落脚探不到地
  ——抬站高必须同步加深 FeetDown，改站姿几何前先算这条。
- 渲染：腿骨系数 0.60→**0.44** 且**与 `_legGene` 解耦**（基因只调粗细；掺进骨长会把
  同一预设的站立膝角在 141°~97° 间乱抽）。双骨 ≈0.88×JointDist 对 ≈0.83×JointDist
  的站高 → 站立膝内角 ≈141°（等腰余弦：cosθ = 1 − 0.83²/(2×0.44²)），微屈直立。系数收进 `RatFiendJointMath.LegBone`
  单一真相源，渲染件与竞技场命中胶囊改为同调（此前两处各自内联 0.60）。
- 连带：`ArmSwingAmplitude` 0.25→0.35——腿长变大后对侧腿相位归一化变小，等幅补回
  可见摆臂（ARM-SWING 门 0.105m≥0.1）。

**有意的整表重钉**：站立几何是进哈希的物理量，本轮与 R8/R9 的「零漂移」性质相反——
smoke `ExpectedHash` 与矩阵全部 15 条哈希**整表重钉**（流程：置空基线 → 23 项行为门
先全绿 → 取实跑值钉定 → 再全跑一遍对钉值全绿）。爬行/断肢机制未动，相关路线哈希变
只因起跳前的站立段轨迹不同。

**验证**：smoke 16 门 + 4 消融红灯全绿（WALK 里程 68.8m、驼背几何 0.42 未因抬高塌掉；
SEVER/CRAWL/CLIMB 全族行为门在新站高下原样通过）；矩阵 23 项全绿；core/smoke 共享层
无漂移。目检：walk 稳态两腿近直、脚落髋正下；run 前倾冲刺腿后蹬展直；早段起身瞬态
（出生爬起+加速）仍矮身，属正常过渡。

**评审补丁（同轮对抗性评审 12 agent，2 误报被核实推翻、1 中危坐实）**：R10 前
「闲置休息位 0.6×腿长 ≈ 站高」纯属巧合——站高抬到 0.83×腿长后，静立零速时 FindGrip
全部采样出可及圈（gaunt 最近采样距髋 0.834 > 腿长 0.8），20 tick 失败进 IdlePose，
`RestHuntPos` 垂脚 0.48 悬空 0.18m。修复走共享层 opt-in（先例 LookaheadTicks）：
`Limb.RestDepth` 默认 0.6 全物种逐位不变（core/smoke 复跑验证），鼠煞装配设
站高/腿长 → 闲置脚正好垂到地面。带静立段的四条矩阵路线（crawl-step / sever-all /
attack / sever-during-attack）哈希随之二次重钉，其余 11 条与 smoke DET 不受影响
（IdlePose 只在无移动意图 + 连续找不到落点时触发）。

## 5.9 R11 步态修复轮（2026-08-22，用户实测报告）

**症状**：移动时双腿始终前倾、高频小碎步——正常人走路是双腿一前一后大跨步、低频，
不会两条腿都斜在身前。R10 修好了**站**，这轮修**走**。

**根因（无引擎探针实测，三个独立缺陷叠加）**：
1. **推进天花板与油门无关**：`ApplyLocomotionForce` 的余量钳制用固定 `MaxMoveSpeed`，
   RunSpeed≥0.15 后注入率全被钳制吸收——RunSpeed 0.35/0.6/1.0 三档巡航速度**逐位相同**
   （0.0859/tick = 3.44 m/s）。「走」就是用跑的速度在挪。
2. **前瞻圈把释放点推到髋前**：Lookahead 释放条件 |锁点 − (髋+vel×前瞻)| > 腿长，
   锁点在地面（髋下 0.68m），垂直分量吃掉腿长预算后水平余量只剩
   sqrt(0.8²−0.68²)≈0.42m；前瞻 10 × vel 0.086 = 圈心在髋前 0.86m（LimitLength 0.8）
   → 释放点 = 髋前 0.8−0.42 ≈ **+0.4m**。脚从落点（+0.75）到释放（+0.4）**全程在髋前**
   （实测 bothAhead=100%、11.4 步/秒）——这就是「双腿前倾碎步」的全部几何。
3. **落点甩满腿长**：FindGrip 的 goal = 髋 + dir×腿长，FeetDown 0.75 时 dir 下压角仅
   ~13°，goal 水平前距 0.94×腿长（0.75m）——落点远超站高允许的水平可及（0.42m），
   脚被腿长钳制拖行数 tick 才真正踩上。

**修复**（全部鼠煞物种内部，共享层零改动）：
- **天花板 ∝ 油门**（控制器）：`ceiling = MaxMoveSpeed × RunSpeed` → 巡航 ≈
  0.9×天花板×油门。gaunt 走(0.35) 1.21 m/s / 中速(0.6) 2.06 / 跑(1.0) 3.44（与修复前
  满速一致，追击手感不变）。
- **前瞻 10→3**（工厂）：走姿释放点落到**髋后 ≈0.33m**（跑 ≈髋后 0.14m）——脚过髋后蹬。
  §3.5 的硬性要求是「前瞻**循环**（>0）」不是取值 10。
- **FeetDown 0.75→4.5**（参数）：步向下压角 ≈54°（人迈步的髋-落点连线本来就近乎垂下），
  落点收到髋前 ≈0.46m ≈ 0.9×水平可及。副作用是探针带整体贴地：goal 从地上 0.49m 降到
  0.06m，**下行落差余量从 0.06m 放宽到 ≈0.5m**（R10 边界②就地解除），代价是上行覆盖
  从 0.84m 收到 ≈0.4m（走路撞高台阶的落脚未验证，见边界）。
- **反相自举**（控制器新参数 `LegPhaseMinGap` 0.2m）：前瞻循环的落点是公共的（髋前
  0.46m），两腿间距塌到近同相时纯时间错位被逐周期原样保留——出生 stagger 仅 0.1m，
  长步幅慢步频下会走成双脚并排的「并步跳」（旧 6-tick 短周期里 guard 每周期都咬合，
  天然错相；29-tick 长周期下 guard 永不触发）。规则：两脚同时踩稳、沿步向间距 <0.2m
  且居后脚已过髋 → 居后脚 `ForceRelease` 提前重迈。一次干预拉开 ≈半步幅，之后
  释放-重落循环自稳在近反相（实测错相比 0.34~0.60 恒定、无周期漂移），规则静默。
- **连带**：yank 路线的「恢复行走」度量从**末刻位移**改为**累计里程**——巡逻乒乓下
  末刻位移是周期敏感量，速度分离改变巡逻节奏后路线末端恰好折返回落点（|Δ|=1.46m），
  把正常恢复（总里程 87m）误判成未恢复。

**实测步态（无引擎探针，gaunt）**：走(0.35) 落点+0.46/释放−0.30、2.67 步/秒、错相 0.60；
跑(1.0) 6.7 步/秒、错相 0.50。broad 走 0.88 m/s、1.6 步/秒大跨步（落点+0.52/释放−0.44）；
whelp 满速 4.07 m/s、13 步/秒疾碎（幼体质感）。目检：站立腿伸直、摆动腿屈膝、异侧摆臂
（ARM-SWING 反相从 30% 回到 74%、摆幅 0.235m）。

**有意的整表重钉（本轮第二次）**：速度与步态进哈希——smoke `ExpectedHash =
0x65C6F16F2AE5BCAC` + 矩阵 15 条全部重钉（流程同 R10：置空 → 23 项行为门全绿 →
取实跑值钉定 → 复跑对钉值全绿）。core/smoke 共享层无漂移（Limb 未改动）。

## 5.10 R13 直立调头修复轮（2026-08-22，用户实测报告）

**症状**：直立行走中意图反向时，头直接从躯干里穿到另一侧、背部装饰（驼峰/椎骨凸）
跟着打转——R8 修爬行调头时同一病灶的直立版（当时裁定「站立瞬时置向，腿+站立力偶
立刻把身体带过来」，头伺服与渲染 dorsal 被漏出裁定外：Facing 瞬间翻 180°，头伺服把
头沿直线拽穿躯干，渲染 dorsal（−Facing）低通沿最短路径打转）。

**修复**（全部鼠煞局部，两件）：
- **Facing 限速回转扩展到直立**（新出生参数 `StandTurnRatePerTick`）：`SlewFacing`
  分支从 `CanStand ? effMove : slew(...)` 收敛为统一 slew、按姿态取速率。夹角 ≤ 限速时
  直接取目标——直线与小角度转向逐位同旧行为。初版 0.12 rad/tick（2×爬行，180° ≈
  0.65s）用户实测嫌弧大，**收紧到 0.22（≈12.6°/tick，180° ≈ 0.36s）**换原地掉头的
  干脆感；上限 ~0.3——再快逼近瞬时置向的头穿躯干带（爬行消融红灯 ③ 的病灶）。
- **直立推进/走倾沿回转后的 Facing**（`standDrive`）：沿原始 effMove 推会在意图反向时
  先全速倒车再转身——R8「胸沿 Facing 推」的直立对应件。头伺服/驼背姿态轴本就读
  Facing，自动跟随画弧。

**半径收紧（R13b，用户反馈「时间够快、半径再小」）**：只提转速治不了横甩——探针逐
tick 轨迹坐实两条被推翻的初判：① **线性节流（注入 ∝ 对齐度）无效**——BaseSpeed(0.065)
≫ 天花板余量，min() 钳制下部分对齐即近全额注入；② **只刹身后半球不够**——天花板沿
**新朝向**量余量，收不住侧向残速，注入沿旋转中的 Facing 逐 tick 矢量叠加把出弯速度
弹到 4.6 m/s 超巡航、出弯后侧向残速只靠 0.9 阻尼慢放、尾段再横漂 0.5m。终版 =
**双态转体门**（`TurnAlignGate` 0.9 ≈ 26°）：对齐度 < 门 → 推进零注入 + 主动刹水平速度
（`TurnBrakePerTick` 0.8，与 chunk 阻尼叠成 0.72/tick；空中不刹，击飞弹道不受染指）；
过门 → 全油门沿新方向按本种弹射起步体质（2 tick 到巡航）加速。探针实测（gaunt）：
冲刺掉头横甩 **0.97→0.19m**、前向过冲 0.245m；走档横甩 0.34→0.10m；掉头时间不变
（14 tick ≈ 0.36s）。直线对齐度恒 1、缓弯被 slew 逐 tick 追平（≥cos 12.6°≈0.976），
双双不触发——只有 >26° 急转吃这道门；到点判定半径 0.4m 远大于横甩，无绕圈风险
（矩阵 walk 航点、run travel 门实跑验证）。目检（run 路线 ±14m 乒乓的 U 弯连拍）：
滑步刹停 → 原地拧身（头绕体外扫过，无穿插）→ 反向弹射。

**有意的整表重钉（流程同 R10/R11；本轮三连调各重钉一次，终版为准）**：Facing/速度
进哈希——smoke `ExpectedHash = 0x1B0FC96919D881E9` + 矩阵 15 条重钉，23 项对钉值
复跑 GREEN。**纯爬行/静立路线（crawl-step / sever-both-legs / sever-all /
broad-sever-leg）哈希与 R11 逐位相同**——直立转体门只分叉直立转向路径，爬行链与
dusk-parity 无扰；core/smoke 共享层无漂移。

## 5.11 R15 走姿慢摆修复轮（2026-08-22，用户实测报告）

**症状**：走路步频合适，但单步「抬腿→落下」的摆动过程太快——腾空段仅 ~3 tick
（40Hz 下 75ms），像瞬移。**根因**：`LegSpeed`（= Limb.HuntSpeed，摆动期每 tick
逼近上限）是静态出生参数，一个值同时伺候走/跑两档——可行域下界（§6.5 速度 ≥0.12）
与跑档敏捷度都逼着它取高值（gaunt 0.3），走档只能跟着快。

**修复**（全部鼠煞局部：`TickLegs` 一处调制 + 三个出生参数）：摆动期 HuntSpeed 按
Gait 调制——`LegSwingSpeedFactor`（gaunt/whelp 0.45；broad 0.55 守 §6.5 下界
0.24×0.55=0.132）走端全额缩放，`LegSwingBlendStart/End`（0.62/0.95）越窗线性恢复
全速。混合窗**刻意比 RunBlend 姿态窗（0.35~0.95）更靠后**：walk 档（RunSpeed 0.6 →
Gait≈0.6）必须落在全额减速带内，复用姿态窗会被 r≈0.42 稀释近半。踩住的脚在吸附
分支被钳在落点、不受缩放影响；手法 = TickArms 摆臂 `ArmSwingSpeedFactor` 先例的腿版。

**爬行豁免（矩阵红灯抓到的真回归，按 `CrawlFactor` 混回全速）**：初版无条件生效，
矩阵 `crawl-step`（驱动 RunSpeed 0.6）卡台阶前（chestY 0.21 = 消融签名）——爬行
蹬地是推进链的一部分，慢摆削抓地占空比。smoke 爬行断言全部驱动 RunSpeed 1.0
（Gait 越窗后豁免与否同值）故未暴露，只有矩阵路线露出来。CrawlFactor 是既有平滑
标量：直立慢摆、爬行全速、摔倒过渡期连续。

**量化（新增 smoke `[WALK-SWING]`，含消融红灯）**：walk 档腾空段（Gripping==false
连续段，含 3 tick GripDelay）6.0→10.1 tick——真实腾空 ~3→~7 tick（75→175ms）；
run 档 6.0 不变（<walk 断言钉走跑分化）；步周期 18.0→22.1 tick（+23%——释放保序
guard 等对脚落稳，摆动加长直接顺延周期；±35% 窗断言钉「慢的是摆动、不是步频塌陷」）；
walk 里程 61.9m/800tick 无衰减。消融（factor=1）腾空段缩回 6.0（>1.4× 红灯）。

**有意的整表重钉（流程同 R13）**：腿轨迹进哈希——smoke
`ExpectedHash = 0x5235F7C11CB39443` + 矩阵 15 条重钉，23 项对钉值复跑 GREEN。
调参口：嫌步频掉了 → `LegSwingSpeedFactor` 上调（0.45→0.6 效果折半）；嫌还不够慢
→ 下调但守 §6.5 `LegSpeed×factor ≥ 0.12`。

## 5.12 R16 凝视目标轮（2026-08-22，用户实测报告）

**症状**：竞技场里怪近身时头垂下去——追杀感断裂。**根因**：头 aim 只由 Gait 姿态
混合决定（耷拉↔平视按 RunBlend），而竞技场近身油门刻意收到 0.35（远快近慢防冲过头）
→ Gait 落到姿态窗底端 → 头进入全额耷拉；「正在追杀谁」这个信息根本没有进头伺服的
输入面。

**修复（内核 opt-in 输入 `LookTarget: Vector3?`，§6.6 惯例）**：非 null 且清醒时 =
猎杀姿态——头 aim 覆盖为直视该点，且（R16b 续报「手也耷拉」后）走/跑手臂混合权重
下限抬到 `LookArmRaise`（0.85，仅 LookTarget 设置时生效 → 参数天然哈希中立）：低速
逼近手保持前伸备抓，站定凝视也进走/跑分支不落身侧。它与 GrabTarget 的本质区别保留
——不追点抓取（误用 GrabTarget 会让双臂全程满伸抓空气），爬行撑地仍优先于抬臂
（推进链不可劫持）。优先级 GrabTarget > LookTarget > 爬行看路 > 姿态混合。生命周期
镜像 GrabTarget：Shift 平移、Teleport 作废。张嘴不进内核——竞技场用既有
`MouthDrive` 接口常开 `ChaseMouthDrive`（0.6）即可，零新耦合。

**渲染侧两处（R16b，化妆层私有）**：① `DrawHead` 的 gaze 公式里 `downBias`
（Lerp(0.50, −0.05, runN)）是纯化妆分量，被抓/凝视时 runN→0 会强压 0.5 竖直分量、
盖过物理头伺服的「直视目标」——GrabTarget/LookTarget 非空时整段绕开，gaze 直指
目标（物理偏移 0.25 垫底防抖）；② 张嘴改**双颌对开**：颅骨绕颌铰点后仰承担
`UpperGapeShare = 0.35` 份额的动态张角、下颌承担其余（此前只掉下巴，嘴缝中线永远
比鼻尖低半个张角，全开读成「头朝下」；jawRest 静息微张全归下颌，站姿吻部不上翘；
铰点/headRight 轴不动 → 咬合点稳定，牙眼耳各随其帧）。竞技场配套：Strike/Grabbed
全程 MouthDrive ≥ ChaseMouthDrive、咬合窗拉满——被抓时看到的是正对镜头的大张嘴。

**验证**：smoke `[LOOK-TARGET]`（低速走姿头轴点积 stare=+0.30 vs droop=−0.70 +
手前伸 0.83m vs 0.14m，消融红灯 = 不喂即回耷拉基线；Shift/Teleport 生命周期）；
**零重钉**——默认 null、回归路线从不设置，smoke `ExpectedHash` 与矩阵 15 条哈希
原值复跑 GREEN 两轮（R16/R16b 各一次，这正是 opt-in 纪律买到的东西）。攻击路线
截图目检：静息帧闭嘴颅骨不翘、咬合帧沿视线轴对开大口。竞技场无头全流程复验：
追→扑→抓→咬循环完好，且起手扑抓命中更快（手已举在瞄准线附近，grabbed 与
strike start 同 tick 达成）。

## 5.13 R17 断臂垂位轮（2026-08-22，用户实测报告）

**症状**：断臂残肢在站立时垂向胯部而不是自然下垂；双断臂时正面看两截残肢并拢成
「倒三角围脖」。

**根因**：`RatArm` 的臂长钳制/腋窝排斥锚都在**胸心**（单粒子手的既有抽象，完整臂
无碍——垂手分支给的是带侧偏的 HuntPos）；但断臂走纯 Dangle（只有自重 + 钳制），
平衡点是**胸心正下方** EffectiveLength 处——两截残肢收敛到同一条中线，而渲染肩关节
画在 `Chest.Radius·0.78` 侧偏处，肩→残端连线于是斜指胯部，双断臂正面即倒三角。

**修复（内核，断腿残肢先例的镜像）**：断臂分支保持 Dangle，但在 `RatArm.Tick` 前
注入带肩侧偏的垂位弹簧（复用 `StumpDamping`/`StumpSpring`，目标 = 胸心 +
right·Side·Chest.Radius·0.78 − up·EffectiveLength·0.8，侧偏与渲染肩位同款系数）
——残段被拉回各自肩下，自重/钳制/出地形仍由链尾 `RatArm.Tick` 完成，自由短摆保留。
断腿残肢（髋侧 0.15m 垂位弹簧）从首轮就有这层，臂当时漏了。

**验证**：新 smoke `[ARM-STUMP]`（双断臂站立稳态：残肢各偏本肩侧 0.134m、双残端
相距 0.267m、垂在胸下 0.56m；消融红灯 ⑦ = 去掉弹簧即中线收敛）；断臂轨迹进哈希 →
**有意重钉**：smoke `ExpectedHash = 0xA7C06ECCF4698B43` + 矩阵 sever-arm-walk /
sever-all / sever-during-attack 三条（其余 12 条钉定原值复跑通过——臂长钳制单向
「只拉手不推身体」，未断臂路线逐位不动），矩阵 23 项 GREEN；主 core/smoke PASS。

## 5.14 R18 生动性与竞技场玩法轮（2026-08-23，用户实测报告——九项一并收口）

内核唯一改动 = 新增 opt-in `Impact(chunk, Δv)` 接缝（回归路线不调用）——**本轮零重钉**：
smoke 哈希与矩阵 23 项全部按 **R17 钉值**原样 GREEN（本未提交栈相对 HEAD 的哈希变更
全部归属 R15/R17 的有意重钉，见 §5.11/§5.13），主 core/smoke PASS。九项归属：

| # | 症状（用户原话概括） | 根因 | 修复（归属） |
|---|---------------------|------|-------------|
| 1 | 爬行时脑袋倒过来 | 渲染 headUp 参照纯用 `_spineUp`：爬行脊轴转水平后与 headFwd 近共线，去轴残差符号翻向下 → 整头绕视轴滚 180° | up 参照按 CrawlFactor 混向世界上（渲染，§3 R18 轮③） |
| 2 | 取消近身减速；抓住后用锁镜头时间修正站位 | 减速是「防止抓住时站位过近/过远」的旧方案 | Chase 恒满油门；Grabbed 期锁镜头窗内把玩家指数收敛到正面定距 `GrabHoldDistance`（竞技场，§4.3） |
| 3 | 张嘴后喉咙黑区远看像鼻子 | 细长近黑喉锥伸到嘴前缘 | 宽根短锥 + 喉暗→口腔亮红渐变 + Maw 四档调亮（渲染，§3 R18 轮②） |
| 4 | 走跑像平移、木讷 | 躯干无步频耦合的起伏 | 读腿相位的竖直起伏 + 侧摆，脚踩点不动由 IK 吸收（渲染，§3 R18 轮①） |
| 5 | 嘴固定角度木讷 | 张开侧被 clamp(1) 饱和，旧正弦附加不可见 | 负偏置双正弦调制——只朝闭合方向脉动（渲染，§3 R18 轮④） |
| 6 | 咬玩家时该有明显撕咬动作 | 旧咬合脚本反向（蓄势微张→窗口更大张），渲染顿挫挂在 1→0 沿、被推迟到放人后 | 脚本翻转：蓄势大张→闭口=咬中沿；渲染撕扯加强 0.22s/前突 0.09m + 下压 + 侧向撕扯（竞技场+渲染） |
| 7 | 枪击要有部位击退 + 硬直 | 无此机制 | 内核 `Impact` 接缝 + HandleHit 固定序（伤害→击退→断肢→死亡/硬直）；硬直按部位 0.8/0.35/0.5s、期间不追不抓（内核+竞技场） |
| 8 | 血量与死亡表现 | 无此机制 | HP 10、头 ×3、臂/腿逐肢 2 发打空才断（R20）；归零 → `Conscious=false` 倒地涌现 → 全粒子静止 20 tick 尸体永久冻结（竞技场） |
| 9 | 爬行抓不住人/该有控制与咬；断手数影响攻击距离；爬行被抓镜头该朝下 | 瞄点恒为眼高 1.55m——爬行胸高 ~0.3m + 臂长 1.15m **几何上够不到**，扑抓必超时 | 瞄点按 CrawlFactor 降到 `CrawlAimHeight`；攻击门 × 存活手数缩放（1/0.94/0.88）；零手 → Bite 无束缚撕咬（共享冷却与咬合脚本）；镜头对准怪物头 pitch 本就带俯角，瞄点修好后自动成立（竞技场） |

配套：竞技场相位机重构（`Chase/Strike/Grabbed/Bite/Stunned/Dead`，冷却改时间戳、
Recover 相位删除——用户点名「开始考虑架构维护」）；断臂并发钉子改为独立于硬直的
不变量。验证：ratfiend smoke 19 门 GREEN（哈希零漂移）、矩阵 23 项 GREEN（15 条钉定
全 R17 值）、主 core/smoke PASS、竞技场 14s 无头冒烟跑通「满速追 → 扑抓 → 束缚（定距
0.95~1.0m）→ 咬中沿准点 → 放人 → 冷却 → 再抓」闭环、爬行头姿/跑姿张嘴两张截图目检。
硬直/死亡/无手撕咬三条链依赖交互输入，待用户实测。

**R18b 评审轮**（三分区对抗评审 8 初判 → 4 缺陷 + 1 死机制 + 1 文档过时，全部修复）：
① 硬直抢占 Strike/Grabbed/Bite 时漏压攻击冷却——已咬中的 Bite 被枪打断反而免掉 2.5s
冷却、硬直一过立刻再咬（开枪帮怪物提速）；EnterStunned 抢占攻击相位时按「已咬中/
蓄势中」压入完整/短冷却（max 语义，不缩短断臂钉子已压入的时间戳）。② Bite 态撕咬
顿挫永不触发——旧触发器在**平滑域**测「下穿 0.5」，满跑的嘴开度地板 0.15+0.55=0.70
> 0.5；改为直接读宿主咬合脚本 `MouthDrive` 的满开→全闭沿（阈 0.75/0.25，阶跃、不经
低通、帧率无关；追逐龇嘴 0.6→0 的扑空收招不误触）。③ 爬行头倒转修复在尸体上自行
撤销——死亡后 CrawlFactor 衰减归零、趴平尸体退回旧病灶公式；up 参照混合权重改
max(crawl, 躯干轴水平度)，几何事实与意识无关。④ 死亡瞬间清嘴的 0.75→0.15 阶跃会让
刚断气的尸体触发前突撕扯——②的新触发器加 Conscious 门一并堵死。⑤ 放人推离是继承的
死机制——直写 `Velocity` 被玩家「无输入即刻停」语义下一步一步归零、位移恒零；
`ArenaFirstPersonPlayer` 增 opt-in 外部冲量通道（归零/覆写后叠加、0.85/步几何衰减，
3m/s ≈ 净推 0.5m；锁定期不叠加、Place 清零、不用时行为逐帧不变）。⑥ §4.3 残留 R16
「追杀观感」段与 R18 陈述矛盾——按现行为改写。另有牙提亮包络仍按旧 0.12s 归一的
常量漏改（对齐 BiteLungeSeconds）。

- 断肢单向不可逆（无接肢/再生）；重开 = 宿主重建控制器。
- 爬行三肢相位无协调器（手 tick 奇偶 + 腿自身节律自由跑），GripCount 瞬降的推进抖动
  接受为涌现质感——只钉里程不钉平滑度。
- 低台阶爬行已闭环（R9：0.3m 台阶 smoke+矩阵双侧钉死，涌现上限 ≈0.5m）；**斜坡**
  爬行仍未专项验证；连续多级台阶（楼梯）未验证——单级机制自终止后应能续爬下一级，
  待场景实测。
- R10/R11 站走几何的已知瞬态/余量（by-design 或未验证边界）：
  ① 触地/离地瞬间腿满伸（落点髋足距 ≈0.82 > 双骨 0.70），渲染小腿段短暂拉长 ≤~1.3×
  ——R11 大步幅后每步触地都会掠过该瞬态，目检读作「直腿着地」（人走路的脚跟着地
  本就近直腿），中段站立腿（髋足距 ≈0.68）膝角 ≈145° 正常；命中侧由
  LimbAimAssist 0.22m 冗余覆盖；
  ② R11 的 FeetDown 4.5 把落脚探针带整体贴地：**下行**落差余量放宽到 ≈0.5m（R10
  的 6cm 边界就地解除），但**上行**覆盖从 ~0.84m 收到 ≈0.4m——**行走撞上 >0.4m
  高台**时的落脚未专项验证（撞不上就地踏步 vs 翻越，待场景实测；爬行翻台阶走
  手臂链不受影响）。
- 渲染：爬行姿态的整体剪影仍偏「肉堆」（驼峰球贴地占主导）；牙在满口大张时略拥挤；
  均属调参级打磨，待交互实测后按观感收。
- dusk 调色板与 gaunt 的暗/白反转只在 ForBreed 定档层，未做运行时切换。

## 5.15 R19 打击感与爬行扑抓修复轮（2026-08-23，用户实测报告——四项）

内核改动 = 两个 opt-in 接缝（`ImpactTwist` 朝向拧转 + `GrabHandSpread` 抓取双手分开，
默认不调用/为 0，回归路线不涉及；分开量走显式零判——`−0.0 + 0.0 = +0.0` 会翻符号位，
无脑加零向量不保逐位）——**本轮零重钉**：smoke 哈希不变（A7C06ECCF4698B43）、矩阵
23 项 R17 钉值全 GREEN、主 core/smoke PASS。四项归属：

| # | 症状（用户原话概括） | 根因 | 修复（归属） |
|---|---------------------|------|-------------|
| 1 | 硬直期不该垂头垂手——保持进攻姿态，只是不触发攻击判定 | R18 的 Stunned 相位喂全零意图：Gait 衰减 + 闲置垂手分支 → 头耷手垂，挨打像认输 | Stunned 相位删除，硬直 = `_stunUntilTick`（封攻击门）+ `_reelUntilTick`（吃痛回神窗 = min(硬直, `StunReelSeconds` 0.25s)）两个时间戳。回神窗内只掐移动意图，LookTarget/龇嘴照钉（头照瞪、手照举 = LookArmRaise 权重下限）；窗尽恢复满速追逐，整个硬直期攻击门关死（竞技场） |
| 2 | 击退直挺挺后退木讷；打右臂该拧身；打头该带向上冲量 | 全部位冲量都是纯水平枪向；Facing 是伺服目标轴，点质量 chunk 靠冲量拧不出 yaw | 内核新增 `ImpactTwist(yaw, up)`（opt-in）：绕 up 拧 Facing，转向 = 力臂×枪向 的转矩符号（打右臂右肩后拧、打左臂反向，侧后来弹自动正确——纯几何无按侧硬编码；臂 16°/腿 9°）。恢复涌现：回神窗内无意图保持拧姿瞪人，窗尽 SlewFacing（0.22 rad/tick）把朝向甩回猎物。头冲量沿「枪向 + `HitImpactHeadUpBias` 0.6 向上偏置」（≈仰 31°），胸退身分量保持水平（内核+竞技场） |
| 3 | 抓住人时手一直颤抖（镜头抖还是手抖不确定） | **初判被推翻**：先怀疑手的物理振荡/镜头追踪头部微振——束缚稳态实测手与头逐 tick 位移**亚毫米**（首抓收敛瞬态 0.8s 内衰完），物理不抖。真根因 = 两手追同一个 GrabTarget 点**逐位重合**（诊断日志两列全程逐位相同），两套手爪管面/爪刃完全叠置，z-fighting 深度闪烁读成「颤抖」 | 内核 GrabTarget 分支加 `GrabHandSpread` 横向对称分开（opt-in 默认 0 = 旧行为逐位不变；竞技场设 0.09m → 双手分开 0.180m，读成掐住两肩）（内核+竞技场） |
| 4 | 爬行双手在时常提前摆「双手抱住」姿势却抓不住，人能跑走；站着不动等一会才真抓住 | 攻击门 1.8m > 手可及圈 1.40m（臂长 1.15 + `GrabContactRadius` 0.25）的**死区**：站立扑抓靠 3.4m/s 冲刺动量滑进可及圈掩盖了死区；爬行推进弱（CrawlMaxSpeed 1.8m/s）且扑抓期双手离地、推进 ∝ 抓地肢体数当场断供——死区里每 1s 超时空放一轮抱抓、冷却 0.5s 后再来，只有玩家自己站进 1.4m 内才落抓 | 攻击门距按 CrawlFactor 从 `AttackStartRange` 连续收到 `CrawlAttackRange`（1.3m，收进可及圈；**须 ≤1.40m 否则死区复发**，注释已钉）。无头实测爬行 1.21m 起手、出手即中、连续咬合（竞技场） |

配套：相位机再收敛为 `Chase/Strike/Grabbed/Bite/Dead` 五相位——硬直与冷却同构为
时间戳门禁、不占相位（硬直本质是「带门禁的 Chase」；ApplyStun 抢占攻击相位的
放人 + 压冷却逻辑原样保留，R18b 语义不变）。验证（脚本化无头全链）：t=250 追逐中
臂击 → 拧身 16° 方向正确 + 回神 10 tick + 硬直 20 tick；t=480 束缚中臂击 → 抢占放人
+ 满冷却 100 tick 准点复攻；t=600 断腿 → 爬行 1.21m 起手扑抓、连续束缚咬合；t=1400
束缚中头击 → 3 伤毙命 → 倒地 → 32 tick 后尸体冻结；束缚双手分开 0.180m。注意场景
`rat_arena.tscn` 已有用户 Inspector 调参覆盖（HitImpact 0.4/0.3/0.15、命中冗余
0.03/0.03/0.06）——代码默认值只是出厂档。拧身/甩头/回神的手感待用户实测。

**R19b 评审轮**（三分区只读对抗评审 8 初判 → 2 缺陷 + 3 低危 + 2 文档，全部修复）：
① **零手爬行 Bite 死锁**（R19 收门引入的回归）：追逐停靠半径 1.2m 是**水平**距离、
攻击门是胸→瞄点 **3D** 距离——零手爬行档门 1.3×0.88=1.144m < 停靠点 3D 距 ≈1.21m，
怪物对静止玩家在门外 6cm 永久停车瞪视、Bite 永不发起；停靠半径钳到门内侧 ×0.85
（站立各档 ×0.85 仍 >1.2，行为逐位不变——修后无头实测 1.14m 准点起咬、连续咬中）。
② **whelp 爬行死区复发**：CrawlAttackRange 1.3 按 gaunt 臂长 1.15 校准，whelp 臂长
0.85 的可及上限 ≈1.08m < 门——「反复摆抱抓空姿势」在小体格上逐字复发（每 1.5s 一轮
的确定性空抓死循环）；爬行 Strike 档加 min(手感档, (预设臂长+接触余量)×0.95) 钳制
（gaunt 上限 1.314 不触发钳制、逐位不变；whelp 门收到 1.03——修后无头实测 1.01m
起手、次 tick 抓住）。③ 尸体 HUD 攻击冷却继续倒数（stun 已归零、cooldown 漏了，
补 Dead 归零）。④ 两处注释残留已删除的 Recover 相位（按现行为改写）。⑤ 两个 R19
opt-in 零回归覆盖——矩阵与 DET 哈希对 spread>0 / ImpactTwist 路径全盲，后续重构翻转
拧向或压塌分手全绿无感；照 LOOK-TARGET/ARM-STUMP 先例补 smoke 两门：GRAB-SPREAD
（spread=0 双手**逐位重合** sep=0.0 钉成基线事实 + 0.09 分开 0.180m + 两档抓住判定
均成立）与 IMPACT-TWIST（拧角/符号/无意图保持/SlewFacing ≤3 tick 甩回/零轴退化/
连拧 200 次单位长度），smoke 19 门 → **21 门**。⑥ §1.7「接缝只有」清单漏列
LookTarget（与 porting_contract §4.1f 两份真相源互相矛盾，回迁照单接线会漏掉追杀
姿态）。⑦ §3 撕扯触发器描述停在 R18b 之前（仍写 mouthSmoothed 下行沿）。评审另
核实：ApplyStun 与断臂钉子同发命中的固定序收敛、GrabHandSpread=0 逐位等价（静态 +
attack 矩阵路线 C9FA8394023B587F 实跑双确认）、转矩符号两例镜像几何、头冲量 8.2m/s
被脖长约束当 tick 收敛无 NaN、§5.15 全部数字对代码与日志核真。

## 5.16 R20 肢体血量轮（2026-08-23，用户定调——一枪不断肢）

零内核改动（纯宿主竞技场）：臂/腿各设逐肢血量 `LimbHp`（[Export] 默认 2）——命中未断肢体
扣 1，打空才走断肢流；总血量 `MonsterHp` 5 → 10（断完四肢正好 8 发 + 躯干两枪毙命，头
仍 ×3 = 四枪爆头）。扣肢体血与扣总血同发并行；残肢补枪只扣总血；`SeverOnHit=false`
时不扣肢体血、永不断（原「只记命中」语义保持）。HUD 四格从 o/X 改数字/X（斜杠分隔防
两位数歧义），受伤 toast 报 `(剩余/上限)`。

脚本化 headless 全链核真（合成命中注入，事后移除）：R-ARM 两发 hp 9→8、第二发才
`severed ArmRight`；残肢补枪 hp 7 无肢血变化；L-LEG 同构；TORSO/HEAD(×3)/TORSO 收尾
正好 10 点毙命 + 尸体冻结正常。三视角评审（正确性/生命周期/文档）零 real-bug，采纳
三条：① toast 分母改读出生冻结的 `_limbHpMax`（活读导出项，运行中改 Inspector 会骗人）；
② HUD 四格加分隔符；③ §4.3 开头残留「命中臂/腿 → Sever」旧句。不采纳：`SeverOnHit`
更名（注释已准确，名字是断肢系统总开关，`LimbHp=1` 即旧语义）。评审另核实：Strike 期
第一发打臂只伤不断 → 与躯干命中同走 ApplyStun 抢占放人，无悬空态。

## 5.17 R21 短脖轮（2026-08-23，用户目检两刀——鼠头应贴肩）

**症状**：用户指出脖子太长——0.5 的 `NeckLength` 是 Scavenger 式长脖弧（机制蓝本
Humanoid 的遗产，其 0.55 撑的是人形直立头位），驼背 + 42° 耷拉把头挂在一条长弧末端，
观感是「雨世界野人」而不是鼠人。

**首刀被推翻的初判**：第一刀按「球面露出段 = NeckLength −(胸R+头R)」把露出段压到
≈0.04（gaunt 0.38 / whelp 0.31 / broad 0.45 视作本就贴肩不动），目检仍长。用户追问
「是不是垂坠原理挡了短脖」——**不是**：胸↔头是 PullOnly（可压缩）且内核无自碰撞，
头球嵌进胸球完全合法。真正显长的两个原因：
1. 头伺服从**胸球心**沿 42° 耷拉向挂头，NeckLength 里有一整个胸半径埋在躯干内，
   而渲染脖根在**背峰顶**（`chest.Lerp(hips,-0.18)`+dorsal 隆起）——脖根到头这段弧
   不归 NeckLength 管，球面「相切」时头仍吊在背峰轮廓线下方一截。
2. broad 的 0.45 只是球面相切：42° 对角上切点远低于背峰轮廓，照样不显贴肩。

**二刀定稿**（设计量改为「嵌入深度 = (胸R+头R) − NeckLength」，全预设进重叠区）：

| 预设 | NeckLength | 嵌入深度 |
|------|-----------|---------|
| gaunt / dusk | 0.5 → **0.30** | 0.04（头窝进肩峰前缘，吻部从肩间探出） |
| whelp | 0.45 → **0.25** | 0.01 |
| broad | 0.45 → **0.38** | 0.05（正面看头直接嵌在双肩之间） |

纯出生参数：物理链静息长度与头伺服目标球面同步缩，渲染/命中/断口全部跟随 chunk
几何零改动；嵌入区里 PullOnly 恒不激活，头位完全由伺服持有。

**验证**：沙盒 `--ratfiend-screenshot` 四预设定点目检（walk@360/700、run@600，头贴肩、
无穿插）；smoke 全行为断言原样绿（走跑单调、攻击接缝 overreach=0、断肢全族），仅重钉
固定哈希；矩阵 23 项重钉后 GREEN（dusk 与 gaunt 仍逐位同构）。主 smoke（四物种）不受
影响。

**两仓状态**：用户拍板后已于 2026-08-23 同步主仓
`scripts/enemies/kernel/species/ratfiend/`（归化覆盖，diff 仅脖长三处；主仓
RatFiendSmoke 十二段 + 灯光巡拍 + 整游戏播种复验全过）。附带效果：头×3 判定区更贴
躯干，爆头分离度略降——主仓实测手感时留意。

## 5.18 R22 翻越手撑轮（2026-08-25，用户主仓实测报告——翻桌姿势怪、手一直举着）

**症状**：主仓给鼠煞接上家具翻越导航后，用户实测翻桌姿势怪——追击中翻越时手一直
举着。根因两个：① Mount 期 TickArms 分支让双手朝**远侧顶面目标**悬空前伸且两手追同
一点（僵尸举手 + 手爪几何重叠，R19 抓取分手的同款病灶）；手从不接触桌面。② 身体上升
是无条件拽升——手撑不撑桌面对翻越没有任何物理意义。

**机制来源（RW 取证）**：Scavenger **确有真实的手撑翻越机制**——Run 模式下射线找的
`knucklePos` 指关节撑点（`Scavenger.cs:2502` ExactTerrainRayTracePos），在**双 chunk
都离地的翻越瞬间**对躯干施加真实 WeightedPush 力偶（`:2312`，方向随手-身相对位置反转、
强度随速度 0.5→3.5）；Climb 模式另有 swingPos 硬绳单手悬挂。其图形手（ScavengerGraphics
FindGrip）与力学抓点**完全分离、单向依赖**，图形手永不反推身体——与本内核「手是独立
受力点、不反推身体，支撑效果走 HaulChunkUp」同构。本轮 = knucklePos 的 3D 化：撑点用
真射线找、支撑强度接到身体驱动上。

**被消融推翻的初判**：第一版只把拽升强度接手数（`MountHaulBase 0.5 + MountHaulPerHand
0.5×手数`），结果 1.05m 高桌上双手/单手/零手 Mount 段 17/17/50→全部差不多、连**拽升
全归零仍 19 tick 翻上去**——站立翻越的爬升大头根本不是拽升，而是满油门前向推进把球形
chunk 沿箱棱**楔**上去（失重 + 球面 MTD 沿棱角上滑）。手数只接拽升管不住能力；
**前向推进油门必须一起接**（注入与天花板同乘，只压注入会被余量钳制吃回——R11 同款教训）。

**定稿机制**（全部在 MountAndCross 分支内，其余路线逐位不变）：
- 双手 plant-and-trail 撑顶面：探针复用 CrawlProbe* 几何、起点在顶面标高 + 抬升处
  竖直下探，命中低于顶面 `MountSurfaceTolerance`(0.2m) 拒收（不许把途中地板当撑点）；
  未撑住时朝**本手一侧**的桌面落点前伸（横向分开，消举手 + 消重叠）。无 Grounded
  入场门（与爬行撑地相反）：翻越中段身体悬空正是手最该撑的时刻（RW knucklePos 同理）。
- 拽升强度 = 0.5 + 0.5×撑稳手数（1 手 = R22 前等强，2 手 1.5×）。
- 未越顶（髋底 < 顶面 − 死区）时推进油门 = 0.15 + 0.425×撑稳手数（钳 1；双手 = 满速，
  越顶恢复全油门走过顶面）。
- 新观测 `PlantedHandCount`（CrawlGripCount 拆出的手臂分量，一 tick 滞后固定序）。

**验证**：smoke `[RATFIEND-CORE-TRAVERSAL]` 新增高桌三连（1.05m，贴主仓 MaxRise=1.15
门）：Mount 段双手 18 &lt; 单手 24 &lt; 双臂全断 50 tick 严格单调（楔升顽强、物理堵不死
零手，慢 ~3× 即达意；宿主能力门本就拦零臂）+ 油门消融红灯（恒满油让零手塌回 19 tick =
载体证明）+ 矮桌撑手数 2/1 断言；矩阵 traversal 路线加 `planted=2` 硬门并重钉
（439134869AF45272），**其余 15 条基线原样通过** = 泄漏控制。0.82m 桌完成 79→81 tick，
无感。四物种主 smoke 绿。逐帧目检：t18 伸手探桌 → t26 双手撑面顶身 → t34 越顶撑手后拖。

**两仓状态**：用户目检认可后已于 2026-08-25 同步主仓——归化覆盖
`RatFiendParams/RatFiendLocomotionController` 两文件（+129/−24；`RatTraversalIntent`
未动；宿主 `RatFiendTraversalExecutor` 输入面零改动）。主仓复验：RatFiendSmoke 十二段 +
RatFiendTraversalSmoke 七段（S2 实体桌翻越、S3b 80-tick 超时）+ DenseNav + FineGround
全 PASS。随行的沙盒观感件（traversal-loop 循环路线、交互模式自动翻越替身、
`--ratfiend-drive` 钩子）为 lab 专属，主仓寻路执行器本就承担喂意图职责，不迁。

## 6. 回迁主仓（2026-08-23，替换 Shambler）

鼠煞已按 [porting_contract.md](porting_contract.md) §8.3 路线 A + 姿态 2 移植进
`random-room-runtime`，**整体替换**旧 ShamblerEnemy（多实例游荡小怪）：Shambler 的三态
AI 壳（巡逻/闻噪/追击）+ 本仓竞技场攻击层（§4 全套，含 R18~R20 伤害账目）+ 内核逐字
镜像三层拼装；完整记录在主仓 `docs/ratfiend_port.md`（含冒烟踩坑四条与遗留清单）。

**对本仓的影响**：
- `MoveIntentDeadzone` 常量下沉进 `Limb.cs`（蜥蜴控制器留转发 const）——回迁 Limb 不再
  连坐蜥蜴控制器。纯声明位置移动，本仓 193 项矩阵全旧哈希通过。
- **内核在两仓各存一份**（Daddy 同款纪律）：本仓改 `core/species/ratfiend/` 或
  `core/species/lizard/Limb.cs` 后，跑完回归须手动同步主仓
  `scripts/enemies/kernel/species/{ratfiend,lizard}/`。
- **待回同步的主仓侧修正**：正式渲染件 `DrawEyes` 的眼窝球（1.6×eyeR）把眼球椭球整个
  球含在内——眼球从未露出（本仓同 bug 潜伏；主仓已改 1.25×/后靠 0.35/眼球前推 0.55 并
  加自发光）。下轮动渲染件时一并修。
